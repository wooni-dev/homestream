# 이 파일을 .env 로 복사한 뒤 값을 채우세요:  copy .env.example .env

# 스트리밍할 영상 폴더 위치. 따옴표 없이 경로만 적으세요.
SERVE_DIR=D:\Videos

# 접속 비밀번호. AUTH_PASS 를 채우면 인증이 켜지고, 비워두면 인증 없음(같은 Wi-Fi 전용).
# ⚠️ HTTPS 없이 쓰는 비번이라, 외부 접속은 반드시 SSH/WireGuard/Tailscale 같은 암호화 터널 안에서만.
AUTH_USER=admin
AUTH_PASS=
